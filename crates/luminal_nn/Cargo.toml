[package]
name = "luminal_nn"
description = "Neural network backend for luminal"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
itertools = "0.12.1"
luminal = { path = "../.." }
rustc-hash = "1.1.0"
rand = "0.9.2"

[dev-dependencies]
dfdx = { version = "0.13", features = ["f16"] }
paste = "1.0.14"
candle-core = "0.8"
